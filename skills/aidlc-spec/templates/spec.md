# Spec: <title>

- Slug: `<yyyy-mm>-<kebab-topic>`
- Intent: `docs/features/<slug>/intent.md` (approved <yyyy-mm-dd>)
- Date: <yyyy-mm-dd>
- Policies in force: <list the skills/policy documents applied, with versions if known>
- Status: draft | awaiting approval | approved

## Requirements

Numbered and testable. Each one traceable to a line in the intent.

## Out of scope

Carried from the intent's non-goals, plus anything ruled out during design.

## Design

How the requirements are met in this codebase. Name the actual modules, endpoints, tables,
and files. Include data shapes and interface changes.

## Policy application

For each policy applied (security, brand, UX, compliance), one line on what it required and
how the design satisfies it.

## Flagged concerns

Areas needing a decision from a policy owner before build starts — especially where two
policies contradict each other. If there are none, write "None." Never omit this section.

## Open questions carried from intent

Each open question from the intent, marked answered (with the answer) or still open.
