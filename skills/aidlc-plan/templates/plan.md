# Plan: <title>

- Slug: `<yyyy-mm>-<kebab-topic>`
- Intent: `docs/features/<slug>/intent.md` (<yyyy-mm-dd>)
- Spec: `docs/features/<slug>/spec.md` (approved <yyyy-mm-dd>)
- Date: <yyyy-mm-dd>

## Files that change

Each path with one line on what changes in it. New files marked as new.

## Order of work

Numbered steps in the order they will be done, with the reason for the ordering where it is
not obvious (a dependency, a migration that must land first).

## Risks

What could break, what is hard to reverse, and which step carries the most risk. Include
alternatives considered and why they were rejected.

## Proof

How each step will be shown to work: the command to run, the test that must pass, the
observable behaviour. One entry per step.
